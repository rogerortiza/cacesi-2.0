'use strict';

angular.module('cacesi.directives')
	.directive('inspeccion_extintores', function() {
		return {
			templateUrl : '',
			restrict : 'E',
			scope : {
				data : '='
			},
			.controller : ['$scope', '$timeout', function($scope, $timeout) {

			}]
		}
	})
;
